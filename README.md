# Region Sprites

Public, generated pixel-art sprites for poke-issues in The Region. Generated deterministically from issue content via `region/tools/sprites.py` in the (private) fernie repo. Public only so they render inline inside private-repo issue bodies.
